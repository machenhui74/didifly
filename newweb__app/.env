# =============================================================================
# 🔐 Newweb__app 环境变量配置文件示例
# =============================================================================
# 
# 📝 说明：
# 1. 复制此文件为 '.env' 并修改相应的值
# 2. 生产环境必须修改所有默认值，特别是 SECRET_KEY
# 3. 所有路径使用正斜杠 '/' 或双反斜杠 '\\'
# 4. 确保 '.env' 文件不被提交到版本控制系统
#
# =============================================================================

# -----------------------------------------------------------------------------
# 🚨 核心安全配置（生产环境必须修改）
# -----------------------------------------------------------------------------

# Flask 应用密钥 - 用于会话加密和安全功能
# ⚠️  生产环境必须设置为强随机字符串（建议64位以上）
# 生成命令: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev_secret_key_please_change_in_production_newweb

# Flask 运行环境
# 可选值: development, production, testing
FLASK_ENV=development

# 应用调试模式（生产环境必须设置为 false）
FLASK_DEBUG=true

# -----------------------------------------------------------------------------
# 📁 数据存储路径配置
# -----------------------------------------------------------------------------

# 数据文件夹根目录（用户数据、配置文件存储）
# 示例: ./data （相对路径，推荐）
# 示例: /app/data （绝对路径）
DATA_FOLDER=./data

# 源文件夹路径（训练资料来源）
# Windows 示例: D:\\训练资料
# Linux 示例: /home/user/training_materials
SOURCE_FOLDER=./data/source_materials

# 目标文件夹路径（生成的学员训练方案存储位置）
# Windows 示例: D:\\学员训练方案
# Linux 示例: /home/user/student_training_plans
DESTINATION_FOLDER=./data/student_training_plans

# -----------------------------------------------------------------------------
# 📊 报告和文档配置
# -----------------------------------------------------------------------------

# 报告模板文件路径（.docx 格式）
# 确保文件存在且可读
REPORT_TEMPLATE_PATH=./data/templates/test_report_template.docx

# 报告输出文件夹（生成的测评报告存储位置）
REPORT_OUTPUT_FOLDER=./data/reports

# -----------------------------------------------------------------------------
# 🏃‍♂️ 感统训练方案配置
# -----------------------------------------------------------------------------

# 训练动作数据库文件路径（.xlsx 格式）
# 包含所有可用的训练动作和参数
TRAINING_ACTION_DB_PATH=./data/action_database.xlsx

# 训练方案输出文件夹（生成的个性化训练方案）
TRAINING_PLAN_OUTPUT_FOLDER=./data/training_plans

# -----------------------------------------------------------------------------
# 🗄️ 数据库配置（未来迁移使用）
# -----------------------------------------------------------------------------

# 数据库连接 URL
# SQLite 示例: sqlite:///./data/app.db
# PostgreSQL 示例: postgresql://user:password@localhost:5432/newweb_app
# DATABASE_URL=sqlite:///./data/app.db

# 数据库连接池配置
# DB_POOL_SIZE=5
# DB_MAX_OVERFLOW=10

# -----------------------------------------------------------------------------
# 🔧 高级配置选项
# -----------------------------------------------------------------------------

# 日志级别
# 可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 日志文件路径
LOG_FILE=./logs/app.log

# 文件上传大小限制（MB）
MAX_UPLOAD_SIZE=50

# Session 超时时间（小时）
SESSION_TIMEOUT_HOURS=2

# 学员档案数量限制
MAX_STUDENT_PROFILES=100000

# -----------------------------------------------------------------------------
# 🌐 Web 服务器配置
# -----------------------------------------------------------------------------

# 应用运行端口
PORT=5000

# 应用绑定地址
HOST=127.0.0.1

# 工作进程数（生产环境）
WORKERS=4

# -----------------------------------------------------------------------------
# 📧 通知配置（可选）
# -----------------------------------------------------------------------------

# 邮件服务器配置
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=true
# MAIL_USERNAME=your_email@gmail.com
# MAIL_PASSWORD=your_app_password

# 管理员邮箱
# ADMIN_EMAIL=admin@example.com

# -----------------------------------------------------------------------------
# 🔗 外部服务配置（可选）
# -----------------------------------------------------------------------------

# Redis 缓存配置
# REDIS_URL=redis://localhost:6379/0

# API 密钥和第三方服务
# OPENAI_API_KEY=your_openai_api_key
# ANALYTICS_TRACKING_ID=GA_TRACKING_ID

# =============================================================================
# 📚 环境变量说明文档
# =============================================================================
#
# 🔴 必须配置的变量：
# - SECRET_KEY: 生产环境必须设置强密钥
# - FLASK_ENV: 设置正确的运行环境
# - 所有路径变量: 根据实际部署环境调整
#
# 🟡 建议配置的变量：
# - LOG_LEVEL: 根据环境设置合适的日志级别
# - DATABASE_URL: 数据库迁移后使用
# - MAIL_*: 启用邮件通知功能
#
# 🟢 可选配置的变量：
# - REDIS_URL: 启用缓存功能
# - 外部 API 密钥: 集成第三方服务
#
# ============================================================================= 